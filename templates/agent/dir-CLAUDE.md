# CLAUDE.md — [Directory / Module Name]

> Local context for this module. Read before editing anything here.

---

## What this module owns

[One sentence. What is this directory responsible for?]

## What it must never do

- [Hard constraint — e.g. "Never write to the database directly — go through the service layer"]
- [Security constraint — e.g. "Never log request bodies — they may contain PII"]

## Key files

| File | Purpose |
|---|---|
| `index.ts` | Entry point / public API of this module |
| | |

## How to test changes here

```bash
[command to run tests scoped to this module]
```

## Known gotchas

[What does every new person get wrong about this module?]

## Dependencies

- **Depends on:** [other modules/services this one calls]
- **Depended on by:** [what breaks if this module fails]

## Open questions

[Any unresolved unknowns specific to this module — link to `docs/open-questions.md` if tracked there]
